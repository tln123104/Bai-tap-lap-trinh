#include<bits/stdc++.h>
using namespace std;

int main()
{
    double a, b, c;
    cin >> a >> b >> c;
    if(a > 0 && b > 0 && c > 0)
        cout << "YES";
    else    cout << "NO";
    return 0;
}
